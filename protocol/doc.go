// Package protocol defines Sentry's wire envelopes, payload categories, DSNs,
// and SDK metadata. Custom transports can inspect and serialize these types
// without depending on the SDK's buffering and delivery implementations.
package protocol
